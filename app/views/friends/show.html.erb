<%= render @friend %>

<div>
<%= link_to "Edit", edit_friend_path(@friend), class: 'btn btn btn-secondary' %> 
<%= link_to "Back", friends_path, class: 'btn btn-dark' %> <br><br>
<%= button_to "Delete", @friend, method: :delete, data: { confirm: 'Are you sure?'}, class:"btn btn-danger" %>
</div>